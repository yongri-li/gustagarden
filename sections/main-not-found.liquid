<script>
  window.onload = function () {
    var currentURL = encodeURIComponent(window.location.href);
    var targetURL = `https://n8n.codext.de/webhook-test/gustagarden.com?url=${currentURL}`;

    fetch(targetURL)
      .then(response => response.json())
      .then(data => {
        console.log('Request successful:', data);
        window.location.href = '/';
      })
      .catch(error => console.log('Request failed', error));
  };
</script>


<section class="section--flush-bottom">
  <div class="section__color-wrapper">
    <div class="empty-state">
      <span class="empty-state__background-text">404</span>

      <div class="text-container">
        <h3 class="heading h3">{{ '404.general.title' | t }}</h3>
        <p>{{ '404.general.description' | t }}</p>

        <div class="button-wrapper">
          <a
            href="{{ routes.root_url }}"
            class="button button--primary">
            {{- '404.general.back_to_home' | t -}}
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "404",
  "class": "shopify-section--404"
}
{% endschema %}
